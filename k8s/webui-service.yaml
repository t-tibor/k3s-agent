apiVersion: v1
kind: Service
metadata:
  name: webui
  namespace: k3s-agent
  labels:
    app: webui
spec:
  type: ClusterIP
  selector:
    app: webui
  ports:
    - name: http
      port: 80
      targetPort: http
