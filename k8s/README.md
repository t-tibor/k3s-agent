# Kubernetes manifests

Deployment manifests for the `kubernetes-agent` and `hollama` workloads (see [../docs/ARCHITECTURE.md](../docs/ARCHITECTURE.md) §16).

Not yet populated — this is a placeholder until the Kubernetes deployment phase.
